{
    "default": [
        "build",
        "deploy"
    ],

    "build": [
        "styles",
        "scripts",
        "copy:vendors",
        "svg_sprite"
    ],

    "styles": [
        "sass",
        "postcss",
        "cssmin"
    ],

    "scripts": [
        "concat",
        "uglify"
    ],

    "sync": [
        "browserSync",
        "watch",
        "notify:watch"
    ],

    "deploy": [
        "copy:www",
        "notify:www"
    ]
}
