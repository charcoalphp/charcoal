<section class="card w-100">
    <header class="card-header">
        <h3 class="card-title mb-0">{{# _t }}Application Cache{{/ _t }}</h3>
    </header>
    <div class="card-body">
        <p>{{# _t }}Clearing this cache will clear *all* entries in the application cache pool. In other words, it completely erase all items in the cache (pages, objects and everything else).{{/ _t }}</p>
        <p><small>{{# _t }}It does not clear the compiled templates nor the Cloudflare cache.{{/ _t }}</small></p>
        <p><small>{{# _t }}If the <a href="{{ adminUrl }}system/static-website">static website</a> option is enabled, clearing the cache might not result in new versions of the page, as the static website, when properly configured, supercedes all cache.{{/ _t }}</small></p>
        <p><small>{{# _t }}The <em>Purge</em> function allows the cache to perform basic maintenance tasks, such as removing stale or expired items and reindexing data for improved performance.{{/ _t }}</small></p>
        {{# hasStats }}
        <dl>
            <dt>{{# _t }}Num Entries{{/ _t }}</dt>
            <dd>{{ cacheInfo.app.num_entries }}</dd>
            <dt>{{# _t }}Total Size{{/ _t }}</dt>
            <dd>{{ cacheInfo.app.total_size }}
            <dd></dd>
        </dl>
        {{/ hasStats }}
    </div>
    <footer class="card-footer bg-transparent">
        <button class="btn btn-primary" data-clear="cache" data-cache-type="app">
            <i class="fa fa-trash" aria-hidden="true">&nbsp;</i>
            <span class="btn-label">{{# _t }}Clear Application Cache{{/ _t }}</span>
        </button>
    </footer>
</section>
