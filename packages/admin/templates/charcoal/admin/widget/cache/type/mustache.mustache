<section class="card">
    <header class="card-header">
        <h3 class="card-title mb-0">{{# _t }}Mustache Cache{{/ _t }}</h3>
    </header>
    <div class="card-body">
        {{# cacheInfo.mustache.no_cache_folder }}
        <p>{{# _t }}There is no Cache folder created for Mustache.{{/ _t }}</p>
        {{/ cacheInfo.mustache.no_cache_folder }}
        {{^ cacheInfo.mustache.no_cache_folder }}
        <dl>
            <dt>{{# _t }}Num Entries{{/ _t }}</dt>
            <dd>{{ cacheInfo.mustache.num_entries }}</dd>
            <dt>{{# _t }}Total Size{{/ _t }}</dt>
            <dd>{{ cacheInfo.mustache.total_size }}
            <dd></dd>
        </dl>
        {{/ cacheInfo.mustache.no_cache_folder }}
    </div>
    <footer class="card-footer bg-transparent">
        {{^ cacheInfo.mustache.no_cache_folder }}
        <button class="btn btn-primary" data-clear="cache" data-cache-type="mustache">
            <span class="btn-label">{{# _t }}Clear Mustache Cache{{/ _t }}</span>
        </button>
        {{/ cacheInfo.mustache.no_cache_folder }}
    </footer>
</section>
